apiVersion: v1
kind: Service
metadata:
  name: http-service
  labels:
    app: web-app
spec:
  type: LoadBalancer
  # externalTrafficPolicy: Local
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: web-app
